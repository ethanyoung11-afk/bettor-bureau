# Copy these values into the hosting provider's encrypted secrets panel.
SHARED_APP = "true"
ADMIN_PASSWORD_HASH = "replace-with-a-sha256-password-digest"
ODDSPAPI_API_KEY = "replace-with-your-oddspapi-key"
DATABASE_URL = "postgresql://user:password@host/database?sslmode=require"
